# PyCharm project files
.idea/
*.iml

# Python virtual environments
.venv/
venv/

# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Logs
*.log

# Translations
*.pot

# Django local settings (optional)
local_settings.py

# Media uploads (optional - include if you store user uploads locally)
media/

# Ignore SQLite DB only in teams or CI; allow for solo/local deploy
# Uncomment the next line to exclude DB if using a shared or remote DB
# db.sqlite3

# Allow Django migrations (required for deployment)
# Only ignore compiled `.pyc` within them
**/migrations/*.pyc
**/migrations/__pycache__/

# Allow migration Python files
!**/migrations/*.py
